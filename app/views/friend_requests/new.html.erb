<%= simple_form_for [@profile, @friend_request], url: profile_friend_requests_path(@profile)  do |f| %>
  <%= f.input :request_message %>
  <%= f.submit "Make contact", class: "btn btn-primary" %>
<% end %>
